{
  "name": "task-manager-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for managing tasks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.464.0",
    "@aws-sdk/util-dynamodb": "^3.464.0",
    "uuid": "^11.0.3"
  }
}
